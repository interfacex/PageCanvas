# 隐私说明

PageCanvas 默认在浏览器本地处理网页。完整导出和传统提取不会上传网页内容。

只有用户主动选择“AI 智能提取”时，插件才会把清洗后的标题、正文、列表及来源地址发送到用户自行配置的模型服务。脚本、样式、Cookie、LocalStorage、表单值和浏览历史不会随请求发送。数据处理规则由该模型服务提供方决定。

API Key 仅保存在 `chrome.storage.local`，由设置页写入、后台服务工作线程读取；不会写入导出图片、任务数据、日志或安装包。用户可在设置页覆盖或通过清除扩展数据删除。

本版本不接入百度网盘或其他云盘，不上传导出图片。
